import { Toaster as SonnerToaster } from 'sonner';

// TODO: Add customisation for placement and theme
const Toaster = () => {
    return <SonnerToaster richColors theme={'dark'} position={'bottom-right'} />;
};

export default Toaster;
